function f(entity) {
	local a = (b) ? c : d
	local z = someVeryLongFunctionName(argumentOne, argumentTwo, argumentThree, argumentFourXX)

	local ok = aaa || bbb
	local w = anotherVeryLongFunctionName(argumentOne, argumentTwo, argumentThree, argumentFour)
	return ok
}
